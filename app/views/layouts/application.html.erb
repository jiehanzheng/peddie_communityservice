<!DOCTYPE html>
<html>
<head>
  <title>PeddieCommunityservice</title>
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <%= stylesheet_link_tag    "application", media: "all", "data-turbolinks-track" => true %>
  <%= javascript_include_tag "application", "data-turbolinks-track" => true %>
  <%= csrf_meta_tags %>

  <!-- HTML5 Shim and Respond.js IE8 support of HTML5 elements and media queries -->
  <!--[if lt IE 9]>
    <script src="https://oss.maxcdn.com/libs/html5shiv/3.7.0/html5shiv.js"></script>
    <script src="https://oss.maxcdn.com/libs/respond.js/1.3.0/respond.min.js"></script>
  <![endif]-->
</head>
<body>

<nav class="navbar navbar-default navbar-static-top" role="navigation">
  <div class="navbar-header">
    <button type="button" class="navbar-toggle" data-toggle="collapse" data-target="#app_nav">
      <span class="sr-only">Toggle navigation</span>
      <span class="icon-bar"></span>
      <span class="icon-bar"></span>
      <span class="icon-bar"></span>
    </button>
    <%= link_to "Community Day Signups", root_path, :class => 'navbar-brand' %>
  </div>

  <div class="collapse navbar-collapse" id="app_nav">
    <% if Rails.env.development? %>
      <ul class="nav navbar-nav">
        <li><p class="navbar-text">Development</p></li>
        <li><p class="navbar-text"><%= Time.now %></p></li>
      </ul>
    <% end %>
    <div class="navbar-right">
      <ul class="nav navbar-nav">
        <li>
          <% if current_user %>
            <%= link_to bootstrap_glyphicon(:user) + ' ' + "Sign out" + ' ' + current_user.first_name, sessions_destroy_path, :method => :post %>
          <% else %>
            <%= link_to bootstrap_glyphicon(:user) + ' ' + "Sign in", signin_path(request.original_url) %>
          <% end %>
        </li>
      </ul>
    </div>
  </div>
</nav>

<div class="container">
  <% if params[:redirected_from_peddie].present? %>
    <div class="alert alert-info alert-dismissable">
      <button type="button" class="close" data-dismiss="alert" aria-hidden="true">&times;</button>
      <strong>Why was I redirected to jiehan.org?</strong> In order to protect your account information against <a class="alert-link" href="http://en.wikipedia.org/wiki/Man-in-the-middle_attack" target="_blank">man-in-the-middle attacks</a>, you have been redirected to Jiehan&rsquo;s server where secure connection (HTTPS) was enforced.
    </div>
  <% end %>

  <% if current_user != nil %>
    <%= eligibility_notice(current_user) %>
  <% end %>
  
  <%= breadcrumbs :style => :bootstrap %>

  <%= bootstrap_flash %>
  
  <%= yield %>

  <div id="footer" role="footer">
    <p class="text-muted">
    This sign up site is an <%= link_to "open source project", 'https://github.com/jiehanzheng/peddie_communityservice', :target => '_blank' %> by Jiehan Zheng &rsquo;14.<br />
    We are looking for a few developers and maintainers for next year.&nbsp;&nbsp;<%= link_to "Send me an email", 'mailto:jzheng-14@peddie.org', :target => '_blank' %> if you are interested!</p>
  </div>
</div>

</body>
</html>
